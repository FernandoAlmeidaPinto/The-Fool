# Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish the visual identity (Ivory & Charcoal + shadcn/ui), Docker-based MongoDB, Auth.js v5 authentication, and health check endpoint for The Fool tarot platform.

**Architecture:** Next.js 16 App Router fullstack monolith. Domain logic lives in `lib/`, UI in `app/`, components in `components/`. Auth.js v5 with Mongoose adapter for database sessions. Mongoose singleton for serverless connection reuse.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind CSS 4, shadcn/ui, Auth.js v5, Mongoose, MongoDB 7, bcryptjs, Docker Compose

**Next.js 16 breaking changes to respect:**
- `middleware.ts` is renamed to `proxy.ts` with `export function proxy()`
- `headers()`, `cookies()`, `params`, `searchParams` are **async** — always `await` them
- Always read `node_modules/next/dist/docs/` before using any Next.js API to check for changes

---

## File Map

```
docker-compose.yml                    # MongoDB 7 service
.env.example                          # Documented env vars
.env.local                            # Local env (gitignored)
lib/db/mongoose.ts                    # Mongoose singleton connection
lib/auth/auth.ts                      # Auth.js v5 config, providers, callbacks
lib/auth/auth-actions.ts              # Server Actions: register, login, logout
lib/users/model.ts                    # User Mongoose schema
app/globals.css                       # Ivory & Charcoal CSS variables (override shadcn defaults)
app/layout.tsx                        # Root layout with Geist font (already exists, modify)
app/page.tsx                          # Landing — redirect to /auth/login if unauthenticated
app/api/health/route.ts               # Health check endpoint
app/api/auth/[...nextauth]/route.ts   # Auth.js route handler
app/auth/login/page.tsx               # Login page
app/auth/register/page.tsx            # Register page
components/ui/                        # shadcn/ui components (auto-generated)
```

---

### Task 1: shadcn/ui Init + Ivory & Charcoal Design Tokens

**Files:**
- Modify: `app/globals.css`
- Modify: `app/layout.tsx`
- Create: `components.json` (auto-generated by shadcn init)
- Create: `lib/utils.ts` (auto-generated by shadcn init)
- Create: `components/ui/` (directory)

**Docs to check:** `node_modules/next/dist/docs/01-app/01-getting-started/06-css.md`

- [ ] **Step 1: Initialize shadcn/ui**

```bash
npx shadcn@latest init
```

When prompted, select:
- Style: Default
- Base color: Neutral (we'll override)
- CSS variables: Yes

This creates `components.json`, `lib/utils.ts`, and modifies `globals.css`.

- [ ] **Step 2: Override CSS variables in `app/globals.css` with Ivory & Charcoal palette**

Replace the shadcn default CSS variables with:

```css
@import "tailwindcss";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar-background: var(--sidebar-background);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --radius: var(--radius);
}

:root {
  --background: oklch(0.98 0.005 80);      /* #FAFAF7 */
  --foreground: oklch(0.22 0.01 60);        /* #2C2C2C */
  --card: oklch(0.94 0.01 70);              /* #F0EDE8 */
  --card-foreground: oklch(0.22 0.01 60);   /* #2C2C2C */
  --popover: oklch(0.98 0.005 80);          /* #FAFAF7 */
  --popover-foreground: oklch(0.22 0.01 60);/* #2C2C2C */
  --primary: oklch(0.35 0.015 60);          /* #4A4540 */
  --primary-foreground: oklch(0.98 0.005 80);/* #FAFAF7 */
  --secondary: oklch(0.94 0.01 70);         /* #F0EDE8 */
  --secondary-foreground: oklch(0.35 0.015 60);/* #4A4540 */
  --muted: oklch(0.89 0.012 65);            /* #E2DCD5 */
  --muted-foreground: oklch(0.58 0.02 60);  /* #8A7F72 */
  --accent: oklch(0.75 0.025 65);           /* #B8A99A */
  --accent-foreground: oklch(0.22 0.01 60); /* #2C2C2C */
  --destructive: oklch(0.55 0.22 25);       /* #DC2626 */
  --destructive-foreground: oklch(0.98 0.005 80);
  --border: oklch(0.89 0.012 65);           /* #E2DCD5 */
  --input: oklch(0.89 0.012 65);            /* #E2DCD5 */
  --ring: oklch(0.58 0.02 60);              /* #8A7F72 */
  --chart-1: oklch(0.58 0.02 60);
  --chart-2: oklch(0.75 0.025 65);
  --chart-3: oklch(0.35 0.015 60);
  --chart-4: oklch(0.89 0.012 65);
  --chart-5: oklch(0.22 0.01 60);
  --radius: 0.625rem;
  --sidebar-background: oklch(0.98 0.005 80);
  --sidebar-foreground: oklch(0.22 0.01 60);
  --sidebar-primary: oklch(0.35 0.015 60);
  --sidebar-primary-foreground: oklch(0.98 0.005 80);
  --sidebar-accent: oklch(0.75 0.025 65);
  --sidebar-accent-foreground: oklch(0.22 0.01 60);
  --sidebar-border: oklch(0.89 0.012 65);
  --sidebar-ring: oklch(0.58 0.02 60);
}
```

Note: shadcn/ui with Tailwind v4 uses oklch colors. The hex values in comments are approximate references to the Ivory & Charcoal palette. The implementer should run `npx shadcn@latest init` first to see the exact format generated, then replace the color values while keeping the structure intact.

- [ ] **Step 3: Verify `app/layout.tsx` uses Geist font**

The existing boilerplate should already have Geist configured. Verify it imports from `next/font/google` or `geist` package and applies to the body. Do not change if it already works.

- [ ] **Step 4: Install shadcn/ui components needed for auth pages**

```bash
npx shadcn@latest add button input card label
```

- [ ] **Step 5: Verify the dev server renders with the new palette**

```bash
yarn dev
```

Open `http://localhost:3000` — verify the background is the warm ivory (`#FAFAF7`), not white. Text should be dark charcoal.

- [ ] **Step 6: Commit**

```bash
git add app/globals.css app/layout.tsx components.json lib/utils.ts components/ tailwind.config.ts postcss.config.mjs
git commit -m "feat: init shadcn/ui with Ivory & Charcoal design tokens"
```

Note: file list may vary depending on what shadcn init creates. Stage all new/modified files.

---

### Task 2: Docker Compose + Environment Variables

**Files:**
- Create: `docker-compose.yml`
- Create: `.env.example`
- Create: `.env.local`

- [ ] **Step 1: Create `docker-compose.yml`**

```yaml
services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
```

- [ ] **Step 2: Create `.env.example`**

```bash
# MongoDB
MONGODB_URI=mongodb://root:root@localhost:27017/the_fool?authSource=admin

# Auth.js
AUTH_SECRET=<generate with: openssl rand -base64 32>
AUTH_GOOGLE_ID=<Google OAuth client ID>
AUTH_GOOGLE_SECRET=<Google OAuth client secret>

# App
NEXTAUTH_URL=http://localhost:3000
```

- [ ] **Step 3: Create `.env.local` from example**

Copy `.env.example` to `.env.local` and generate a real `AUTH_SECRET`:

```bash
openssl rand -base64 32
```

Set `AUTH_GOOGLE_ID` and `AUTH_GOOGLE_SECRET` to placeholder values for now (Google OAuth won't work until real credentials are configured).

- [ ] **Step 4: Verify MongoDB starts**

```bash
docker compose up -d
docker compose ps
```

Expected: mongodb service running, port 27017 mapped.

- [ ] **Step 5: Commit**

```bash
git add docker-compose.yml .env.example
git commit -m "feat: add Docker Compose for MongoDB and env example"
```

Note: `.env.local` is gitignored by the existing `.env*` rule.

---

### Task 3: Mongoose Connection Singleton

**Files:**
- Create: `lib/db/mongoose.ts`

- [ ] **Step 1: Install mongoose**

```bash
yarn add mongoose
```

- [ ] **Step 2: Create `lib/db/mongoose.ts`**

```typescript
import mongoose from "mongoose";

const MONGODB_URI = process.env.MONGODB_URI;

if (!MONGODB_URI) {
  throw new Error("MONGODB_URI environment variable is not defined");
}

interface MongooseCache {
  conn: typeof mongoose | null;
  promise: Promise<typeof mongoose> | null;
}

declare global {
  // eslint-disable-next-line no-var
  var mongooseCache: MongooseCache | undefined;
}

const cached: MongooseCache = global.mongooseCache ?? {
  conn: null,
  promise: null,
};

if (!global.mongooseCache) {
  global.mongooseCache = cached;
}

export async function connectDB(): Promise<typeof mongoose> {
  if (cached.conn) {
    return cached.conn;
  }

  if (!cached.promise) {
    cached.promise = mongoose.connect(MONGODB_URI);
  }

  cached.conn = await cached.promise;
  return cached.conn;
}
```

- [ ] **Step 3: Verify connection works**

Ensure Docker MongoDB is running (`docker compose up -d`), then test by adding a temporary log in a route or running a quick script. This will be fully validated by the health check in Task 6.

- [ ] **Step 4: Commit**

```bash
git add lib/db/mongoose.ts package.json yarn.lock
git commit -m "feat: add Mongoose singleton connection for serverless reuse"
```

---

### Task 4: User Model + Auth.js v5 Configuration

**Files:**
- Create: `lib/users/model.ts`
- Create: `lib/auth/auth.ts`
- Create: `app/api/auth/[...nextauth]/route.ts`

**Docs to check:**
- `node_modules/next/dist/docs/01-app/02-guides/authentication.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`

- [ ] **Step 1: Install auth dependencies**

```bash
yarn add next-auth@beta @auth/mongoose-adapter bcryptjs
yarn add -D @types/bcryptjs
```

Note: Auth.js v5 is published as `next-auth@beta`. Check the latest version at install time.

- [ ] **Step 2: Create `lib/users/model.ts`**

```typescript
import mongoose, { Schema, models, model } from "mongoose";
import type { Model } from "mongoose";

export interface IUser {
  _id: mongoose.Types.ObjectId;
  name: string;
  email: string;
  emailVerified: Date | null;
  password?: string;
  image: string | null;
  createdAt: Date;
  updatedAt: Date;
}

const UserSchema = new Schema<IUser>(
  {
    name: { type: String, required: true },
    email: { type: String, required: true, unique: true },
    emailVerified: { type: Date, default: null },
    password: { type: String, select: false },
    image: { type: String, default: null },
  },
  { timestamps: true }
);

export const User: Model<IUser> =
  models.User ?? model<IUser>("User", UserSchema);
```

Note: `password` has `select: false` so it's excluded from queries by default. Auth credentials flow will use `.select("+password")` explicitly.

- [ ] **Step 3: Create `lib/auth/auth.ts`**

```typescript
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";
import Google from "next-auth/providers/google";
import { MongoDBAdapter } from "@auth/mongoose-adapter";
import bcrypt from "bcryptjs";
import mongoose from "mongoose";
import { connectDB } from "@/lib/db/mongoose";
import { User } from "@/lib/users/model";

export const { handlers, signIn, signOut, auth } = NextAuth({
  adapter: MongoDBAdapter(mongoose.connection.getClient(), {
    databaseName: "the_fool",
  }),
  session: { strategy: "database" },
  pages: {
    signIn: "/auth/login",
  },
  providers: [
    Google({
      clientId: process.env.AUTH_GOOGLE_ID,
      clientSecret: process.env.AUTH_GOOGLE_SECRET,
    }),
    Credentials({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" },
      },
      async authorize(credentials) {
        await connectDB();

        const email = credentials.email as string;
        const password = credentials.password as string;

        if (!email || !password) return null;

        const user = await User.findOne({ email }).select("+password");
        if (!user || !user.password) return null;

        const isValid = await bcrypt.compare(password, user.password);
        if (!isValid) return null;

        return {
          id: user._id.toString(),
          name: user.name,
          email: user.email,
          image: user.image,
        };
      },
    }),
  ],
  callbacks: {
    async session({ session, user }) {
      if (session.user && user) {
        session.user.id = user.id;
      }
      return session;
    },
  },
});
```

Important: The implementer must check the actual `@auth/mongoose-adapter` API at install time. The adapter may require the mongoose connection or the native MongoClient — adjust based on the installed version's docs.

- [ ] **Step 4: Create `app/api/auth/[...nextauth]/route.ts`**

```typescript
import { handlers } from "@/lib/auth/auth";

export const { GET, POST } = handlers;
```

- [ ] **Step 5: Verify auth config loads without errors**

```bash
yarn dev
```

Open `http://localhost:3000/api/auth/providers` — should return JSON with `google` and `credentials` providers.

- [ ] **Step 6: Commit**

```bash
git add lib/users/model.ts lib/auth/auth.ts app/api/auth/\[...nextauth\]/route.ts package.json yarn.lock
git commit -m "feat: add User model and Auth.js v5 config with credentials + Google"
```

---

### Task 5: Register and Login Pages

**Files:**
- Create: `lib/auth/auth-actions.ts`
- Create: `app/auth/register/page.tsx`
- Create: `app/auth/login/page.tsx`
- Modify: `app/page.tsx`

**Docs to check:**
- `node_modules/next/dist/docs/01-app/01-getting-started/11-data-mutations.md` (Server Actions)
- `node_modules/next/dist/docs/01-app/02-guides/authentication.md`

- [ ] **Step 1: Create `lib/auth/auth-actions.ts` — register Server Action**

```typescript
"use server";

import bcrypt from "bcryptjs";
import { connectDB } from "@/lib/db/mongoose";
import { User } from "@/lib/users/model";
import { signIn } from "@/lib/auth/auth";

export async function register(formData: FormData) {
  const name = formData.get("name") as string;
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;

  if (!name || !email || !password) {
    return { error: "All fields are required" };
  }

  if (password.length < 8) {
    return { error: "Password must be at least 8 characters" };
  }

  await connectDB();

  const existingUser = await User.findOne({ email });
  if (existingUser) {
    return { error: "Email already registered" };
  }

  const hashedPassword = await bcrypt.hash(password, 12);

  await User.create({
    name,
    email,
    password: hashedPassword,
  });

  await signIn("credentials", {
    email,
    password,
    redirectTo: "/",
  });
}

export async function loginWithCredentials(formData: FormData) {
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;

  if (!email || !password) {
    return { error: "Email and password are required" };
  }

  try {
    await signIn("credentials", {
      email,
      password,
      redirectTo: "/",
    });
  } catch (error) {
    // Auth.js throws NEXT_REDIRECT on success — rethrow it
    if ((error as Error).message?.includes("NEXT_REDIRECT")) {
      throw error;
    }
    return { error: "Invalid email or password" };
  }
}

export async function loginWithGoogle() {
  await signIn("google", { redirectTo: "/" });
}

export async function logout() {
  const { signOut } = await import("@/lib/auth/auth");
  await signOut({ redirectTo: "/auth/login" });
}
```

- [ ] **Step 2: Create `app/auth/register/page.tsx`**

```tsx
import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Label } from "@/components/ui/label";
import { register } from "@/lib/auth/auth-actions";

export default function RegisterPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-center text-2xl">Create Account</CardTitle>
        </CardHeader>
        <CardContent>
          <form action={register} className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="name">Name</Label>
              <Input id="name" name="name" type="text" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" name="email" type="email" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <Input id="password" name="password" type="password" minLength={8} required />
            </div>
            <Button type="submit" className="w-full">
              Register
            </Button>
          </form>
          <p className="mt-4 text-center text-sm text-muted-foreground">
            Already have an account?{" "}
            <Link href="/auth/login" className="text-primary underline">
              Sign in
            </Link>
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
```

- [ ] **Step 3: Create `app/auth/login/page.tsx`**

```tsx
import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Label } from "@/components/ui/label";
import { loginWithCredentials, loginWithGoogle } from "@/lib/auth/auth-actions";

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-center text-2xl">Sign In</CardTitle>
        </CardHeader>
        <CardContent>
          <form action={loginWithCredentials} className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" name="email" type="email" required />
            </div>
            <div className="space-y-2">
              <Label htmlFor="password">Password</Label>
              <Input id="password" name="password" type="password" required />
            </div>
            <Button type="submit" className="w-full">
              Sign In
            </Button>
          </form>
          <div className="relative my-4">
            <div className="absolute inset-0 flex items-center">
              <div className="w-full border-t border-border" />
            </div>
            <div className="relative flex justify-center text-xs uppercase">
              <span className="bg-card px-2 text-muted-foreground">or</span>
            </div>
          </div>
          <form action={loginWithGoogle}>
            <Button type="submit" variant="outline" className="w-full">
              Sign in with Google
            </Button>
          </form>
          <p className="mt-4 text-center text-sm text-muted-foreground">
            Don&apos;t have an account?{" "}
            <Link href="/auth/register" className="text-primary underline">
              Register
            </Link>
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
```

- [ ] **Step 4: Update `app/page.tsx` to redirect unauthenticated users**

```tsx
import { auth } from "@/lib/auth/auth";
import { redirect } from "next/navigation";
import { logout } from "@/lib/auth/auth-actions";
import { Button } from "@/components/ui/button";

export default async function HomePage() {
  const session = await auth();

  if (!session?.user) {
    redirect("/auth/login");
  }

  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-background">
      <h1 className="text-2xl font-semibold text-foreground">
        Welcome, {session.user.name}
      </h1>
      <p className="mt-2 text-muted-foreground">
        You are signed in as {session.user.email}
      </p>
      <form action={logout} className="mt-4">
        <Button type="submit" variant="outline">
          Sign Out
        </Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 5: Test the full auth flow manually**

```bash
docker compose up -d
yarn dev
```

1. Open `http://localhost:3000` → should redirect to `/auth/login`
2. Click "Register" → fill form → submit → should land on home page with welcome message
3. Click "Sign Out" → should redirect to login
4. Login with the credentials just created → should land on home page

- [ ] **Step 6: Commit**

```bash
git add lib/auth/auth-actions.ts app/auth/ app/page.tsx
git commit -m "feat: add register and login pages with auth flow"
```

---

### Task 6: Health Check Endpoint

**Files:**
- Create: `app/api/health/route.ts`

**Docs to check:** `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`

- [ ] **Step 1: Create `app/api/health/route.ts`**

```typescript
import { NextResponse } from "next/server";
import { connectDB } from "@/lib/db/mongoose";
import mongoose from "mongoose";

export async function GET() {
  try {
    await connectDB();
    const dbState = mongoose.connection.readyState;

    if (dbState === 1) {
      return NextResponse.json({
        status: "ok",
        db: "connected",
        timestamp: new Date().toISOString(),
      });
    }

    return NextResponse.json(
      {
        status: "error",
        db: "disconnected",
        timestamp: new Date().toISOString(),
      },
      { status: 503 }
    );
  } catch {
    return NextResponse.json(
      {
        status: "error",
        db: "disconnected",
        timestamp: new Date().toISOString(),
      },
      { status: 503 }
    );
  }
}
```

- [ ] **Step 2: Verify health check works**

```bash
curl http://localhost:3000/api/health
```

Expected: `{"status":"ok","db":"connected","timestamp":"..."}`

- [ ] **Step 3: Verify health check reports error when MongoDB is down**

```bash
docker compose stop
curl http://localhost:3000/api/health
```

Expected: `{"status":"error","db":"disconnected","timestamp":"..."}` with 503 status.

Then restart MongoDB:
```bash
docker compose up -d
```

- [ ] **Step 4: Commit**

```bash
git add app/api/health/route.ts
git commit -m "feat: add health check endpoint with MongoDB status"
```

---

### Task 7: Update CLAUDE.md + Final Cleanup

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Update CLAUDE.md commands section**

Update the commands to use `yarn` and add new commands:

```markdown
## Commands

```bash
yarn dev             # Start dev server (Next.js with hot reload)
yarn build           # Production build
yarn start           # Start production server
yarn lint            # ESLint (flat config, no args needed)
docker compose up -d # Start MongoDB
docker compose down  # Stop MongoDB
```
```

- [ ] **Step 2: Update "Current state" section in CLAUDE.md**

Replace "create-next-app boilerplate" with a description of what now exists: design system, auth, MongoDB, health check.

- [ ] **Step 3: Run lint and build to catch any issues**

```bash
yarn lint
yarn build
```

Fix any errors before committing.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md with yarn commands and current project state"
```

---

## Completion Checklist

After all tasks are done, verify:

- [ ] `docker compose up -d` starts MongoDB without errors
- [ ] `yarn dev` starts the app without errors
- [ ] `http://localhost:3000` redirects to `/auth/login`
- [ ] Register flow works (creates user in MongoDB)
- [ ] Login flow works (credentials)
- [ ] Logout flow works
- [ ] `GET /api/health` returns `{ status: "ok", db: "connected" }`
- [ ] UI uses Ivory & Charcoal palette (warm ivory background, charcoal text, earthy accents)
- [ ] `yarn build` succeeds
- [ ] `yarn lint` passes
